class Logger {

  log(str) {
    console.log(str);
  }

  info(str) {
    console.info(str);
  }

  debug(str) {
    console.debug(str);
  }

  error(str) {
    console.error(str);
  }

  warn(str) {
    console.warn(str);
  }

}

module.exports = Logger;
